# [COLAB / GPU] The training + export stack. Install in the Colab notebook.
# Versions move fast — if something breaks, have Claude Code reconcile against
# what's actually installed (`pip show trl transformers peft bitsandbytes`).
torch
transformers>=4.44
datasets
accelerate
peft>=0.12
trl>=0.11
bitsandbytes
# Not used directly, but Colab preinstalls an old torchao that recent peft
# versions hard-reject (ImportError) during PeftModel.from_pretrained's LoRA
# dispatch, even though this pipeline never touches torchao quantization.
torchao>=0.16.0
pyyaml
sentencepiece
protobuf
